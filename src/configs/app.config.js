const appConfig = {
  apiPrefix: "/api",
  authenticatedEntryPath: "/app/sales/dashboard",
  unAuthenticatedEntryPath: "/sign-in",
  tourPath: "app/sales/dashboard",
  enableMock: true,
};

export default appConfig;
